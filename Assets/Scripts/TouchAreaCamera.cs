using System.Collections;
using System.Collections.Generic;
using UnityEngine.UI;
using UnityEngine;

public class TouchAreaCamera : MonoBehaviour
{
    public Image touchAreaImage;        // Imagem da UI que define a área de toque
    public RectTransform touchAreaRect; // O RectTransform da área de toque

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
